# Pinned to the versions that match the local working venv. Loose floors
# (>=) caused a Streamlit Cloud deploy failure where a newer anthropic
# release was being picked up.

streamlit==1.57.0
anthropic==0.97.0
httpx==0.28.1
python-dotenv>=1.0
pyyaml>=6.0
pandas==2.3.3
openpyxl==3.1.5
pydantic==2.13.3
pymupdf==1.27.2.3
mcp>=1.0
